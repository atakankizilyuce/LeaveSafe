{
    "name": "leavesafe-site",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "The landing page. Nothing here is built — this exists so the demonstration can be tested.",
    "scripts": {
        "test": "vitest run",
        "coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^5.0.0",
        "jsdom": "29.1.1",
        "vitest": "^5.0.0"
    }
}
