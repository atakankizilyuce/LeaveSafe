{
  "name": "leavesafe-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phone interface for LeaveSafe, built into dist/ and embedded in the Go binary.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@preact/signals": "^2.11.2",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@preact/preset-vite": "^2.10.1",
    "@vitest/coverage-v8": "^5.0.1",
    "jsdom": "30.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
